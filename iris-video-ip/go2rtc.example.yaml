# Template only. Store the real configuration outside Git with mode 0600.
api:
  listen: ":1984"
rtsp:
  listen: ":8554"
streams:
  ip_camera: rtsp://REPLACE_IN_PRIVATE_SECRET
# No FFmpeg transcode by default. Browser H.265 support must be validated.
